# ملاحظات اختبار واجهة Soli Medical

تاريخ الاختبار: 2026-08-14.

الرابط المختبر: https://solimedical.github.io/1/?ui-check=5350b70

تم فتح النسخة المنشورة بحساب المدير الافتراضي للاختبار، وظهرت صفحة إعدادات النظام والأسعار والقوائم. البطاقات الأمنية الثلاث تظهر مستقلة ومغلقة افتراضياً: القفل التلقائي بعد عدم النشاط، النسخ الاحتياطي والاستعادة اليدوية، وسجل العمليات الأخير. كما ظهرت بطاقات الحسابات والصلاحيات والأسعار والتحصيل وتصنيفات المصروفات والقوائم الطبية الأساسية مغلقة افتراضياً.

الملاحظة البصرية: ظهور الإطارات المنقطة والأرقام الملونة في لقطة المتصفح هو تمييز أدوات الفحص للعناصر التفاعلية، وليس جزءاً من تصميم التطبيق.

الخطوة التالية: فتح البطاقات واحدة تلو الأخرى، ثم اختبار صفحة الروشتة على مقاسات سطح المكتب والهاتف والتحقق من عدم وجود تداخل أو بطاقات مفتوحة تلقائياً.

نتيجة إضافية: تم فتح بطاقة النسخ الاحتياطي من المتصفح، وظهرت أزرار تنزيل نسخة JSON والاستعادة والتنبيه داخل البطاقة نفسها دون تداخل مع بطاقة القفل أو سجل العمليات. اللقطة الحالية تُظهر القفل والنسخ الاحتياطي مفتوحين بسبب الاختبار التفاعلي، بينما سجل العمليات وباقي الأقسام مغلقة؛ وهذا لا يغيّر الحالة الافتراضية المغلقة عند إعادة تحميل الصفحة.

نتيجة الاختبار التفاعلي: أُغلقت بطاقة القفل التلقائي وبطاقة النسخ الاحتياطي بشكل مستقل، وبقيت بطاقة سجل العمليات وبقية بطاقات الإعدادات مغلقة. لم يظهر تداخل بين البطاقات أو خلل في أزرار الفتح والإغلاق.

نتيجة الفحص البصري على الرابط المنشور: ظهرت بطاقات القفل التلقائي، النسخ الاحتياطي والاستعادة اليدوية، وسجل العمليات الأخير كبطاقات مستقلة متراصة ومغلقة افتراضياً، تليها الحسابات والصلاحيات والأسعار والتحصيل وتصنيفات المصروفات والقوائم الطبية الأساسية. الشريط الجانبي بقي ثابتاً وواضحاً، ولا توجد بطاقة «الروشتة وبيانات العيادة» داخل صفحة إعدادات النظام.

قياس سطح المكتب: عند عرض 1280 بكسل لم يظهر تمدد أفقي؛ إذ تساوى عرض المستند القابل للتمرير مع العرض الداخلي عند 1265 بكسل. عُثر على تسع بطاقات قابلة للفتح والإغلاق وكانت كلها مغلقة بعد إعادة التحميل. ظهر عنوان النسخ الاحتياطي مرتين في الفحص النصي بسبب العنوان والوصف، لا بسبب وجود بطاقة مكررة.

فحص صفحة الروشتة على سطح المكتب: ظهرت بيانات الطبيب والشعار ورابط QR وإعدادات الطباعة ثم حفظ بيانات الطبيب وحفظ القائمة المتكررة وإدارة الفروع. لم يظهر تمدد أفقي في الصفحة، كما بقيت لوحة إعدادات الروشتة ضمن عرض الحاوية دون تمرير أفقي داخلي.

اختبار الهاتف (375×812) على نسخة التطوير المحلية: تم تسجيل الدخول بحساب الاختبار محلياً والتنقل إلى صفحة الإعدادات ثم الروشتة دون كتابة أو تغيير أي بيانات. بقي عرض المستند 375 بكسل مساوياً لعرض إطار العرض في الصفحتين؛ أي لم يُرصد تمدد أفقي. ظهرت بطاقات الإعدادات التسع مغلقة افتراضياً عند الهاتف أيضاً، بما فيها البطاقات الأمنية المستقلة. أثناء الفحص البصري ظهر نص تقني من قالب نافذة القفل قرب أسفل الصفحة؛ تم تتبعه إلى وسم افتتاحي مفقود في نافذة القفل وإصلاحه في نسخة التطوير قبل إعادة الاختبار.

إعادة اختبار الهاتف بعد الإصلاح: اختفى نص القالب وطبقة القفل غير المقصودة من لقطتي الإعدادات والروشتة. ظهرت بطاقات الإعدادات بترتيب واضح وبمسافات مناسبة ضمن عرض الهاتف، كما ظهرت بطاقة بيانات الطبيب وإعدادات الروشتة متجاوبة دون قص أو تمرير أفقي. أكّد الفحص البرمجي أن قيمة templateLeak تساوي false في الصفحتين، وأن البطاقات التسع بقيت مغلقة افتراضياً.
