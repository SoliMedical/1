# دليل تفعيل المزامنة السحابية في Soli Medical

## قبل البدء

إعداد Firebase الخاص بمشروع `clinic1-ba255` موجود في `firebase-config.js`. لا تحتاج إلى وضع أي مفتاح Admin SDK أو كلمة مرور في هذا الملف. المطلوب هو تفعيل مصادقة Email/Password وقاعدة Firestore وإنشاء حسابات Firebase التي تطابق حسابات المستخدمين الموجودة في النظام.

## 1. تفعيل Email/Password

افتح [Firebase Console](https://console.firebase.google.com/) واختر المشروع `clinic1-ba255`. من القائمة افتح **Build → Authentication**، ثم تبويب **Sign-in method**، وفعّل مزود **Email/Password** واضغط **Save**. هذه هي الطريقة الرسمية التي يستخدمها Firebase لإنشاء وتسجيل دخول حسابات البريد وكلمة المرور [1].

## 2. إنشاء حسابات المستخدمين الحالية

أنشئ الحسابات من Firebase Console أو من بوابة الإدارة الموثوقة التي تستخدم Firebase Admin SDK. لكل حساب استخدم البريد الإلكتروني الحقيقي وكلمة مرور Firebase لا تقل عن ستة أحرف، ثم أنشئ عضوية العيادة في `clinics/{clinicId}/members/{uid}` بحالة `active` و`localUserId` مطابق لبطاقة المستخدم. لا تُدخل كلمة المرور في وثيقة `users` ولا في `localStorage`.

إذا كانت بطاقة قديمة تستخدم اسمًا مثل `admin` بدل البريد، سجّل البريد الحقيقي في حقل `firebaseEmail` أو اربط البطاقة بالبريد الموجود في Firebase. لا تعتمد على alias تخميني إذا كان حساب Firebase أُنشئ ببريد مختلف. يجب أن يطابق `firebaseUid` الموجود في بطاقة المستخدم UID الفعلي عند توفره.

احتفظ بكلمة مرور Firebase التي تختارها؛ كلمة المرور المحلية القديمة لا تصبح كلمة مرور Firebase تلقائياً. عند وجود حساب Firebase مسبق، لا تستخدم أداة الإنشاء مرة أخرى، بل استخدم كلمة المرور الحالية لذلك الحساب.

## 3. تسجيل الدخول مع المزامنة

في شاشة الدخول اكتب **البريد الإلكتروني الموجود في Firebase** ثم **كلمة مرور Firebase**. لا يقبل الإصدار الحالي اسم المستخدم المحلي أو كلمة مرور محلية أو جلسة مجهولة. بعد نجاح الدخول يطابق النظام UID والبريد مع بطاقة المستخدم وعضوية العيادة النشطة، ثم يبدأ مستمع Firestore تلقائياً. إذا لم يتوفر اتصال، لا يُنشأ دخول جديد؛ يجب تسجيل الدخول عند عودة الاتصال.

## 4. إنشاء Firestore ونشر القواعد

من Firebase Console افتح **Build → Firestore Database** وأنشئ قاعدة البيانات في وضع **Native** إن لم تكن منشأة. لا تستبدل القواعد الحالية بقواعد عامة. انشر الملف `firestore.rules` الموجود في المستودع بعد مراجعته، لأن المسارات التشغيلية وعضويات العيادة لها قواعد منفصلة وتحظر الكتابة الإدارية غير الموثوقة من العميل.

هذه القواعد تمنع المستخدمين غير المصادق عليهم من قراءة أو تعديل المستند السحابي، وهو النمط الأساسي الذي توثقه Firebase للمستخدمين المصادق عليهم [2]. لكنها تمنح كل حساب Firebase مصادق عليه صلاحية المستند المشترك للعيادة؛ أما صلاحيات الأقسام داخل واجهة Soli Medical فهي صلاحيات واجهة محلية وليست بديلاً عن نظام صلاحيات Firestore على مستوى المستند. إذا احتجت لاحقاً إلى عزل البيانات حسب الدور، فيلزم تصميم قواعد وبيانات منفصل مع Claims أو Backend إداري.

## 5. اختبار نجاح المزامنة

بعد نشر القواعد، سجّل الخروج ثم ادخل بالبريد وكلمة مرور Firebase من شاشة Soli Medical. افتح إعدادات النظام وتحقق من ظهور حالة الحساب المتصل. أضف مريضًا أو زيارة من جهاز أول، ثم افتح النظام من جهاز ثانٍ وسجّل الدخول بحساب Firebase آخر ذي عضوية نشطة. بعد اكتمال المستمع يجب أن تظهر البيانات نفسها. اختبر أيضًا كلمة المرور القديمة `1234` للتأكد من أنها لا تمنح الدخول.

إذا ظهرت رسالة `permission-denied`، راجع نشر Rules وعضوية العيادة النشطة و`localUserId`. وإذا ظهرت رسالة `auth/invalid-credential`، استخدم البريد وكلمة المرور المسجلين في Firebase وليس كلمة المرور المحلية القديمة. لا تضع `FIREBASE_SERVICE_ACCOUNT_JSON` داخل GitHub Pages أو ملفات العميل؛ يستخدم هذا السر فقط في الخادم أو في مهمة إدارية موثوقة. قد تحتاج تغييرات القواعد إلى وقت قصير قبل أن تنعكس على المستمعات النشطة [2].

## 6. النسخ الاحتياطي والاستعادة اليدوية

من **إعدادات النظام → النسخ الاحتياطي والاستعادة اليدوية** اضغط **تنزيل نسخة احتياطية الآن**. سيُنزّل النظام ملف JSON يحتوي على بيانات المرضى والزيارات والوصفات والفواتير والإعدادات والحسابات المحلية. احفظ الملف في مكان خاص ومشفّر؛ فهو قابل للقراءة ولا يُشفّر تلقائياً.

للاستعادة، اضغط **استعادة من ملف JSON** واختر النسخة الصحيحة. راجع التحذير ثم أكد الاستبدال. الاستعادة تخص النسخة المحلية الحالية، لذلك يُنصح بتنزيل نسخة جديدة قبل الاستعادة. هذه الميزة مفيدة عند تعذر الاتصال أو تغيير المتصفح، لكنها لا تستبدل النسخ السحابية المنتظمة.

## المراجع

[1]: https://firebase.google.com/docs/auth/web/password-auth "Firebase: Authenticate with Password-Based Accounts using JavaScript"

[2]: https://firebase.google.com/docs/firestore/security/get-started "Firebase: Get started with Cloud Firestore Security Rules"
