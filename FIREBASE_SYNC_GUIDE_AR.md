# دليل تفعيل المزامنة السحابية في Soli Medical

## قبل البدء

إعداد Firebase الخاص بمشروع `clinic1-ba255` موجود بالفعل داخل `client/index.html`. لا تحتاج إلى استبدال مفاتيح Firebase مرة أخرى. المطلوب هو تفعيل المصادقة وقاعدة Firestore وإنشاء حسابات Firebase التي تطابق حسابات المستخدمين الموجودة في النظام.

## 1. تفعيل Email/Password

افتح [Firebase Console](https://console.firebase.google.com/) واختر المشروع `clinic1-ba255`. من القائمة افتح **Build → Authentication**، ثم تبويب **Sign-in method**، وفعّل مزود **Email/Password** واضغط **Save**. هذه هي الطريقة الرسمية التي يستخدمها Firebase لإنشاء وتسجيل دخول حسابات البريد وكلمة المرور [1].

## 2. إنشاء حسابات المستخدمين الحالية

يوجد داخل النظام مسار أسهل من الإنشاء اليدوي. سجّل دخولك محلياً بحساب المدير، ثم افتح **إعدادات القوائم والأدلة → تفعيل المزامنة السحابية عبر Firebase**. من بطاقة **ترحيل حساب محلي إلى Firebase** اختر المستخدم، أدخل كلمة مرور Firebase جديدة لا تقل عن ستة أحرف، ثم اضغط **إنشاء حساب Firebase لهذا المستخدم**. لا يتم حفظ كلمة المرور الجديدة داخل النظام.

إذا أنشأت الحساب من Firebase Console يدوياً، استخدم البريد الإلكتروني المسجل في خانة بيانات الاتصال عند توفره. وإذا كان الحساب يستخدم اسم مستخدم محلياً بلا بريد، فالحساب `admin` يطابق البريد الداخلي `admin@solimedical.local`. بالنسبة للأسماء العربية أو أي اسم مستخدم غير بريدي، يفضّل استخدام أداة الترحيل داخل النظام بدلاً من تخمين البريد الداخلي.

احتفظ بكلمة مرور Firebase التي تختارها؛ كلمة المرور المحلية القديمة لا تصبح كلمة مرور Firebase تلقائياً. عند وجود حساب Firebase مسبق، لا تستخدم أداة الإنشاء مرة أخرى، بل استخدم كلمة المرور الحالية لذلك الحساب.

## 3. تسجيل الدخول مع المزامنة

في شاشة الدخول اكتب اسم المستخدم المحلي أو البريد الإلكتروني المرتبط بالحساب، ثم اكتب **كلمة مرور Firebase**. بعد نجاح الدخول يطابق النظام حساب Firebase بحساب المستخدم المحلي ويحتفظ بصلاحيات الأقسام الموجودة في النظام، ثم يبدأ مستمع Firestore تلقائياً. إذا لم يتوفر اتصال أو لم ينجح الدخول السحابي، يبقى الوضع المحلي متاحاً على الجهاز.

## 4. إنشاء Firestore ونشر القواعد

من Firebase Console افتح **Build → Firestore Database** وأنشئ قاعدة البيانات في وضع **Native** إن لم تكن منشأة. في تبويب **Rules** ضع القواعد التالية، ثم اضغط **Publish**:

```text
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /soliMedicalApp/sharedClinicData {
      allow read, write: if request.auth != null;
    }
  }
}
```

هذه القواعد تمنع المستخدمين غير المصادق عليهم من قراءة أو تعديل المستند السحابي، وهو النمط الأساسي الذي توثقه Firebase للمستخدمين المصادق عليهم [2]. لكنها تمنح كل حساب Firebase مصادق عليه صلاحية المستند المشترك للعيادة؛ أما صلاحيات الأقسام داخل واجهة Soli Medical فهي صلاحيات واجهة محلية وليست بديلاً عن نظام صلاحيات Firestore على مستوى المستند. إذا احتجت لاحقاً إلى عزل البيانات حسب الدور، فيلزم تصميم قواعد وبيانات منفصل مع Claims أو Backend إداري.

## 5. اختبار نجاح المزامنة

بعد نشر القواعد، سجّل الخروج ثم ادخل بحساب Firebase من شاشة Soli Medical. افتح إعدادات النظام وتحقق من ظهور حالة الحساب المتصل بدلاً من **بانتظار حساب Firebase**. أضف مريضاً أو زيارة من جهاز أول، ثم افتح النظام من جهاز ثانٍ وسجّل الدخول بالحساب الآخر. بعد اكتمال المستمع يجب أن تظهر البيانات نفسها.

إذا ظهرت رسالة `permission-denied`، راجع نشر Rules وتأكد من أن تسجيل الدخول نجح فعلاً. وإذا ظهرت رسالة `auth/invalid-credential`، استخدم البريد وكلمة المرور المسجلين في Firebase وليس كلمة المرور المحلية القديمة. قد تحتاج تغييرات قواعد Firestore إلى وقت قصير قبل أن تنعكس على المستمعات النشطة [2].

## 6. النسخ الاحتياطي والاستعادة اليدوية

من **إعدادات النظام → النسخ الاحتياطي والاستعادة اليدوية** اضغط **تنزيل نسخة احتياطية الآن**. سيُنزّل النظام ملف JSON يحتوي على بيانات المرضى والزيارات والوصفات والفواتير والإعدادات والحسابات المحلية. احفظ الملف في مكان خاص ومشفّر؛ فهو قابل للقراءة ولا يُشفّر تلقائياً.

للاستعادة، اضغط **استعادة من ملف JSON** واختر النسخة الصحيحة. راجع التحذير ثم أكد الاستبدال. الاستعادة تخص النسخة المحلية الحالية، لذلك يُنصح بتنزيل نسخة جديدة قبل الاستعادة. هذه الميزة مفيدة عند تعذر الاتصال أو تغيير المتصفح، لكنها لا تستبدل النسخ السحابية المنتظمة.

## المراجع

[1]: https://firebase.google.com/docs/auth/web/password-auth "Firebase: Authenticate with Password-Based Accounts using JavaScript"

[2]: https://firebase.google.com/docs/firestore/security/get-started "Firebase: Get started with Cloud Firestore Security Rules"
