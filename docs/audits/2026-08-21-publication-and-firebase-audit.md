# تدقيق النشر وFirebase — 21 أغسطس 2026

## حالة النشر عند الفحص

- رابط GitHub Pages الرسمي: `https://solimedical.github.io/1/`.
- أظهر الرابط العام الإصدار `v1.7.7` في واجهة الدخول، وليس `v1.7.8`.
- ملف `index.html` في فرع `main` لمستودع `SoliMedical/1` كان يعلن `v1.7.7` عند الفحص.
- كان سبب الفرق أن مرجع `user_github` المحلي يشير إلى المستودع رقم 2، بينما GitHub Pages يستمد من `SoliMedical/1/main`.
- صُححت وجهة `user_github` المحلية إلى `https://github.com/SoliMedical/1.git`. لا يُدفع إصدار جديد في هذه المرحلة بناءً على طلب المالك تجميع الإصلاحات أولاً.

## قياس Firebase للقراءة فقط

> نُفّذ القياس بواسطة Firebase Admin SDK في وضع قراءة فقط؛ لم تُعدّل أي وثيقة ولم تُعرض بيانات مرضى.

| المؤشر | النتيجة وقت الفحص |
|---|---:|
| الوثيقة القديمة `soliMedicalApp/sharedClinicData` | 14,936 بايت، نحو 0.0142 MiB |
| الوثائق في مخطط V2 المتداخل | 24 وثيقة، تقدير 0.0141 MiB |
| الوثائق في مخطط V3 العلوي | 18 وثيقة، تقدير 0.0092 MiB |
| وضع الترحيل المسجل | `v3_top_level_operational` |

## الاستنتاج الأولي

البيانات الحالية بعيدة جداً عن حد وثيقة Firestore الواحدة، لذلك لا يوجد امتلاء أو بطء ظاهر من الحجم الحالي. لكن التطبيق ما زال يحتفظ بالوثيقة القديمة ومرايا V2/V3 لأسباب الاستعادة، كما أن بعض الاستماعات تقرأ مجموعات تشغيلية كاملة دون صفحات. ينبغي قبل الإصدار القادم تثبيت القراءة والكتابة التشغيلية على V3 فقط، إبقاء النسخ القديمة للقراءة والاستعادة فقط، وإضافة حدود/صفحات لسجلات التدقيق والبيانات التاريخية حتى لا يعود الأداء متأثراً عند نمو العيادة.
