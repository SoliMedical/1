# هيكل مستودع Soli Medical

يوضح هذا الملف وظيفة المجلدات والملفات المتبقية في مستودع `SoliMedical/1`. يعمل الموقع المنشور حالياً كتطبيق HTML واحد على GitHub Pages، بينما يحتفظ المستودع أيضاً بمصدر قالب Manus حتى يمكن تطوير المشروع واختباره لاحقاً دون حذف ملفات البنية الأساسية بصورة غير آمنة.

## مسار التشغيل المنشور

| المسار | الوظيفة | هل هو مطلوب للموقع المنشور؟ |
|---|---|---|
| `index.html` | النسخة الجذرية التي يحمّلها GitHub Pages وتحتوي تطبيق Soli Medical الحالي | نعم |
| `firebase-config.js` | إعدادات Firebase العامة الخاصة بهذه النسخة | نعم |
| `manifest.webmanifest` | تعريف التطبيق القابل للتثبيت واتجاه RTL والأيقونة | نعم |
| `sw.js` | Service Worker للتخزين المؤقت والعمل دون اتصال وتحديث النسخ | نعم |
| `assets/` | الصور والأيقونات المستخدمة في الواجهة والروشتة | نعم |
| `.github/workflows/pages.yml` | رفع محتويات المستودع إلى GitHub Pages بعد كل تغيير على `main` | نعم |

> لا يوضع مفتاح خدمة Firebase Admin أو أي كلمة مرور داخل `firebase-config.js`. هذا الملف يحتوي إعدادات Web العامة فقط.

## مصدر واجهة Soli Medical

| المسار | الوظيفة | الملاحظة |
|---|---|---|
| `client/index.html` | النسخة المصدرية الرئيسية للتطبيق الأحادي التي تُزامن مع `index.html` الجذري | يجب إبقاؤها ومزامنتها عند تعديل التطبيق |
| `client/src/SoliMedical-source.html` | نسخة مصدرية مرجعية محفوظة لتطوير واجهة Soli Medical الأحادية | لا تُحذف أو تُستبدل دون مراجعة الاعتماديات |
| `client/public/` | ملفات عامة صغيرة مرتبطة بقالب التطوير | تُراجع قبل أي حذف |

## ملفات قالب Manus والتطوير

هذه الملفات لا تُحمّل مباشرةً من صفحة GitHub Pages الحالية، لكنها جزء من بيئة التطوير والاختبارات وقابلة للاستخدام عند تحويل التطبيق أو توسيعه. لذلك تُحفظ ولا تُحذف لمجرد أنها غير موجودة في HTML الجذري.

| المسار | الوظيفة |
|---|---|
| `client/src/` | مصدر تطبيق React وقوالب واجهة Manus ومكوّناته |
| `server/` | طبقة الخادم وواجهات tRPC وملفات التكامل والاختبارات |
| `shared/` | الأنواع والثوابت المشتركة بين الواجهة والخادم |
| `drizzle/` | مخطط قاعدة البيانات وملفات الهجرة |
| `package.json` و`pnpm-lock.yaml` | الاعتماديات وأوامر الاختبار والبناء |
| `vite.config.ts` وملفات `tsconfig*` | إعدادات البناء والتحقق من TypeScript |
| `vitest.config.ts` | إعداد اختبارات Vitest |
| `components.json` و`patches/` | إعدادات مكوّنات الواجهة والتوافق مع الاعتماديات |
| `docs/` و`README.md` و`FIREBASE_SYNC_GUIDE_AR.md` | التوثيق والتعليمات التشغيلية |
| `todo.md` | سجل المتطلبات والإصلاحات المنفذة |

## قواعد الحذف المستقبلية

يُسمح بحذف ملف فقط بعد التأكد من عدم وجود مراجع له في HTML أو JavaScript أو إعدادات البناء أو سير عمل GitHub Actions. لا تُحذف ملفات قالب Manus الأساسية لمجرد أنها غير مستخدمة في نسخة GitHub Pages الحالية؛ فوجودها يحافظ على قابلية التطوير والاختبار.

يجب أن تكون ملفات HTML المسموح بها محددة وواضحة: `index.html` و`client/index.html` و`client/src/SoliMedical-source.html`. أي ملف HTML إضافي أو ملف يحمل أسماء مثل `legacy` أو `old` أو `backup` أو `demo` أو `copy` يجب مراجعته قبل دمجه.

## سير النشر

عند دفع commit إلى فرع `main`، يشغّل GitHub Actions فحص المستودع أولاً، ثم يرفع محتوياته إلى GitHub Pages. إذا فشل الفحص بسبب ملف تجريبي أو نسخة HTML إضافية، يتوقف النشر قبل استبدال الموقع الحي.

## مبدأ المزامنة

عند تعديل التطبيق، تُعدّل `client/index.html` أولاً ثم تُزامن إلى `index.html` الجذري. بعد ذلك يجب تحديث رقم الإصدار وService Worker عند الحاجة، وتشغيل اختبارات Vitest، ثم دفع التغيير إلى `main` ومراجعة رابط GitHub Pages.
